name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            archive: tar.gz
          # Cross-compiled from the arm64 macos-latest runner: the deprecated
          # macos-13 Intel runners are starved (the v0.32.0 release job queued
          # ~1h before it was cancelled). CI.yml's wheel job already cross-builds
          # the x86_64 macOS wheel on macos-latest the same way, so this works.
          - os: macos-latest
            target: x86_64-apple-darwin
            archive: tar.gz
          - os: macos-latest
            target: aarch64-apple-darwin
            archive: tar.gz
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            archive: zip

    steps:
      - uses: actions/checkout@v5

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install musl tools (Linux)
        if: matrix.target == 'x86_64-unknown-linux-musl'
        run: sudo apt-get install -y musl-tools

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package (Unix)
        if: matrix.os != 'windows-latest'
        run: |
          STAGING="topica-cli-${{ github.ref_name }}-${{ matrix.target }}"
          mkdir "$STAGING"
          for bin in preprocess analyze train show; do
            cp "target/${{ matrix.target }}/release/$bin" "$STAGING/"
          done
          cp README.md "$STAGING/"
          cp -r examples "$STAGING/"
          tar -czf "$STAGING.tar.gz" "$STAGING"
          echo "ASSET=$STAGING.tar.gz" >> $GITHUB_ENV

      - name: Package (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $staging = "topica-cli-${{ github.ref_name }}-${{ matrix.target }}"
          New-Item -ItemType Directory $staging
          foreach ($bin in @('preprocess','analyze','train','show')) {
            Copy-Item "target/${{ matrix.target }}/release/$bin.exe" "$staging/"
          }
          Copy-Item README.md "$staging/"
          Copy-Item -Recurse examples "$staging/"
          Compress-Archive -Path "$staging/*" -DestinationPath "$staging.zip"
          echo "ASSET=$staging.zip" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          files: ${{ env.ASSET }}
          generate_release_notes: true
